import { useState } from "react";

interface AlbumProps {
    album: string;
}

export function ShowMusic(props: AlbumProps) {
    const [album, setAlbum] = useState<any>(null);

    return (
        <div>

        </div>
    )
}